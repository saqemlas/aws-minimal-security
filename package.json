{
  "name": "@aws-template/account-secure",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf .serverless .esbuild node_modules && yarn cache clean",
    "ci": "yarn install --frozen-lockfile",
    "checktsc": "tsc -noEmit",
    "deploy": "yarn run serverless deploy",
    "remove": "yarn run serverless remove"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "1.7.0",
    "@aws-sdk/client-dynamodb": "3.332.0",
    "@aws-sdk/client-sns": "3.332.0",
    "aws-lambda": "1.0.6",
    "axios": "1.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.83",
    "esbuild": "0.14.51",
    "rimraf": "3.0.2",
    "serverless": "3.30.1",
    "serverless-s3-cleaner": "2.0.2",
    "serverless-esbuild": "1.44.0",
    "serverless-iam-roles-per-function": "3.2.0",
    "typescript": "5.0.4"
  }
}
